{{#extend "stylesheets"}}
  <link rel="stylesheet" href="navStyles.css">
{{/extend}}
{{#extend "scripts"}}
{{/extend}}




{{!-- <nav class="navbar navbar-expand-lg navbar-light bg-light">
  
    <a class="navbar-brand" href="/"><img class= "logo" src= "birdlogo.png"></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
          {{#each data.links}}
          <li class= {{#if this.current}}"nav-item active" {{else}} "nav-item" {{/if}}>
            <a class="nav-link" href="{{this.link}}">{{this.title}} {{#if this.current}} <span class="sr-only">(current)</span> {{/if}}</a>
          </li>
          {{/each}}
      </ul>
    </div>
  </nav> --}}

  <nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/"><img class= "logo" src= "birdlogo.png"></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      {{#each data.links}}
       <a class="nav-item nav-link {{#if this.current}} active {{/if}}" href="{{this.link}}">{{this.title}}</a>
        {{/each}}
    </div>
  </div>
</nav>